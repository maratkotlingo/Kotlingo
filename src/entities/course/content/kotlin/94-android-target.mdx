---
title: "Android Target"
description: "Android target в Kotlin Multiplatform: Android Gradle Plugin, androidMain, Main dispatcher, lifecycle scope, Parcelize, Compose overview, platform dependencies и границы shared-кода."
order: 94
category: "Kotlin Multiplatform"
difficulty: "Продвинутый"
---

import CatTip from "../../ui/CatTip.vue";
import MouseTrap from "../../ui/MouseTrap.vue";

## Android target в KMP

Android target компилирует shared module так, чтобы его можно было использовать в Android application или Android library.

Типичная роль:

- shared business logic in `commonMain`;
- Android-specific storage/network/platform adapters in `androidMain`;
- Android UI in app module or shared Compose module;
- integration with Android lifecycle, resources and permissions;
- `Dispatchers.Main` through Android coroutine module.

```kotlin
plugins {
    kotlin("multiplatform") version "2.4.0"
    id("com.android.library") version "9.0.0"
}

kotlin {
    androidTarget()
}

android {
    namespace = "dev.kotlingo.shared"
    compileSdk = 36

    defaultConfig {
        minSdk = 24
    }
}
```

<CatTip>
Android target — это кошачий мостик между shared логикой и Android-миром: common code идет по центру, а `androidMain` держит Context, Parcelable, lifecycle и Jetpack.
</CatTip>

## `androidMain`

`androidMain` может использовать Android framework and AndroidX.

```text
src/
  commonMain/
    kotlin/
      progress/
        ProgressStore.kt
  androidMain/
    kotlin/
      progress/
        AndroidPreferencesProgressStore.kt
```

Common contract:

```kotlin
// commonMain/kotlin/progress/ProgressStore.kt
package progress

interface ProgressStore {
    suspend fun completedSlugs(): Set<String>

    suspend fun markCompleted(slug: String)
}
```

Android implementation:

```kotlin
// androidMain/kotlin/progress/AndroidPreferencesProgressStore.kt
package progress

import android.content.SharedPreferences
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

class AndroidPreferencesProgressStore(
    private val preferences: SharedPreferences
) : ProgressStore {
    override suspend fun completedSlugs(): Set<String> = withContext(Dispatchers.IO) {
        // SharedPreferences API синхронный, поэтому читаем вне Main thread.
        preferences.getStringSet("completed_slugs", emptySet()).orEmpty()
    }

    override suspend fun markCompleted(slug: String): Unit = withContext(Dispatchers.IO) {
        val current = preferences.getStringSet("completed_slugs", emptySet()).orEmpty()

        preferences.edit()
            .putStringSet("completed_slugs", current + slug)
            .apply()
    }
}
```

`commonMain` знает только `ProgressStore`, а Android details stay in `androidMain`.

<MouseTrap>
Не протаскивай `Context` в common code. Если common code просит Android Context, shared boundary уже течет. Передай interface, value object или конкретный adapter из Android layer.
</MouseTrap>

## Android Gradle Plugin

KMP Android target requires Android Gradle Plugin configuration.

```kotlin
plugins {
    kotlin("multiplatform") version "2.4.0"
    id("com.android.library") version "9.0.0"
}
```

Android block:

```kotlin
android {
    namespace = "dev.kotlingo.shared"
    compileSdk = 36

    defaultConfig {
        minSdk = 24
        consumerProguardFiles("consumer-rules.pro")
    }

    testOptions {
        unitTests.isIncludeAndroidResources = true
    }
}
```

Remember:

- one Android target per Gradle subproject;
- Android app module usually applies `com.android.application`;
- shared Android library module usually applies `com.android.library`;
- Android-specific dependencies belong in `androidMain`;
- Android unit tests belong in `androidUnitTest`;
- instrumented tests belong in Android test source sets depending on project setup.

## Android dependencies

```kotlin
kotlin {
    sourceSets {
        commonMain.dependencies {
            implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.11.0")
        }

        androidMain.dependencies {
            implementation("androidx.core:core-ktx:1.19.0")
            implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.11.0")
            implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.11.0")
        }
    }
}
```

Put AndroidX dependencies in `androidMain`, unless the library explicitly provides a common multiplatform artifact.

Example with Android logger:

```kotlin
// commonMain/kotlin/logging/Logger.kt
package logging

interface Logger {
    fun info(message: String)

    fun error(message: String, throwable: Throwable?)
}
```

```kotlin
// androidMain/kotlin/logging/AndroidLogger.kt
package logging

import android.util.Log

class AndroidLogger(
    private val tag: String
) : Logger {
    override fun info(message: String) {
        Log.i(tag, message)
    }

    override fun error(message: String, throwable: Throwable?) {
        Log.e(tag, message, throwable)
    }
}
```

## Main dispatcher

On Android, `Dispatchers.Main` is backed by the Android main thread when `kotlinx-coroutines-android` is available.

```kotlin
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.launch
import kotlinx.coroutines.withContext

class LessonScreenPresenter(
    private val scope: CoroutineScope,
    private val repository: AndroidLessonRepository
) {
    fun open(slug: String) {
        scope.launch(Dispatchers.Main) {
            renderLoading()

            val lesson = withContext(Dispatchers.IO) {
                repository.loadBlocking(slug)
            }

            renderLesson(lesson)
        }
    }

    private fun renderLoading() {
        println("Показываем загрузку")
    }

    private fun renderLesson(lesson: Lesson) {
        println("Показываем ${lesson.title}")
    }
}

data class Lesson(
    val slug: String,
    val title: String
)

class AndroidLessonRepository {
    fun loadBlocking(slug: String): Lesson {
        return Lesson(slug, "Урок $slug")
    }
}
```

Use lifecycle-aware scopes in app code:

```kotlin
// Android app module example
import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import kotlinx.coroutines.launch

class LessonViewModel(
    private val useCase: OpenLessonUseCase
) : ViewModel() {
    fun open(slug: String) {
        viewModelScope.launch {
            val lesson = useCase.open(slug)
            println(lesson.title)
        }
    }
}
```

Shared KMP module can expose suspending use cases; Android app decides lifecycle scope.

## Parcelize

`Parcelable` is Android-specific. Use `kotlin-parcelize` only in Android source sets or Android app module.

```kotlin
plugins {
    id("kotlin-parcelize")
}
```

Android DTO:

```kotlin
// androidMain/kotlin/navigation/LessonArgs.kt
package navigation

import android.os.Parcelable
import kotlinx.parcelize.Parcelize

@Parcelize
data class LessonArgs(
    val slug: String,
    val source: String
) : Parcelable
```

Common model should not implement `Parcelable`.

```kotlin
// commonMain/kotlin/lesson/LessonId.kt
package lesson

@JvmInline
value class LessonId(
    val value: String
) {
    init {
        require(value.isNotBlank()) { "LessonId не должен быть пустым" }
    }
}
```

Mapping at Android boundary:

```kotlin
// androidMain/kotlin/navigation/LessonArgsMapper.kt
package navigation

import lesson.LessonId

fun LessonArgs.toLessonId(): LessonId {
    return LessonId(slug)
}
```

<MouseTrap>
Не делай common domain model `Parcelable` только ради navigation. Android navigation arguments are adapter-layer concern, not domain contract.
</MouseTrap>

## Compose overview

There are two common architectures:

- native Android UI consumes shared Kotlin use cases;
- Compose Multiplatform shares UI across Android, iOS, desktop and web.

Native Android UI with shared domain:

```kotlin
// commonMain/kotlin/lesson/OpenLessonUseCase.kt
package lesson

class OpenLessonUseCase(
    private val source: LessonSource
) {
    suspend fun open(slug: String): LessonScreenState {
        val lesson = source.load(slug)

        return LessonScreenState.Ready(
            title = lesson.title,
            body = lesson.body
        )
    }
}

interface LessonSource {
    suspend fun load(slug: String): Lesson
}

data class Lesson(
    val title: String,
    val body: String
)

sealed interface LessonScreenState {
    data object Loading : LessonScreenState

    data class Ready(
        val title: String,
        val body: String
    ) : LessonScreenState
}
```

Android Compose consumes it:

```kotlin
// Android app module
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import lesson.LessonScreenState

@Composable
fun LessonScreen(state: LessonScreenState) {
    when (state) {
        LessonScreenState.Loading -> Text("Загрузка")
        is LessonScreenState.Ready -> {
            Text(state.title)
            Text(state.body)
        }
    }
}
```

Compose Multiplatform shared UI can live in `commonMain`, but platform APIs still require platform source sets or expect/actual boundaries.

## Resources and platform APIs

Android resources are Android-specific. Common code should not depend directly on `R.string`.

Bad boundary:

```kotlin
// commonMain
class BadErrorFormatter {
    fun message(): Int {
        // Нельзя возвращать Android resource id из common code.
        // return R.string.error_network
        return 0
    }
}
```

Good boundary:

```kotlin
// commonMain/kotlin/ui/TextMessage.kt
package ui

sealed interface TextMessage {
    data class Plain(
        val value: String
    ) : TextMessage

    data class ResourceKey(
        val key: String
    ) : TextMessage
}
```

Android maps resource keys:

```kotlin
// androidMain/kotlin/ui/AndroidTextResolver.kt
package ui

import android.content.Context

class AndroidTextResolver(
    private val context: Context
) {
    fun resolve(message: TextMessage): String {
        return when (message) {
            is TextMessage.Plain -> message.value
            is TextMessage.ResourceKey -> when (message.key) {
                "network_error" -> context.getString(android.R.string.dialog_alert_title)
                else -> message.key
            }
        }
    }
}
```

## Production checklist

For Android target:

- Android plugin and Kotlin plugin versions are compatible;
- `namespace`, `compileSdk`, `minSdk` are explicit;
- Android-only dependencies are in `androidMain`;
- `Dispatchers.Main` is backed by `kotlinx-coroutines-android`;
- common code does not depend on `Context`, `Parcelable`, `R`, `Activity` or `ViewModel`;
- lifecycle ownership stays in Android layer;
- shared code exposes suspending APIs or observable state;
- Parcelize stays in Android layer;
- Compose sharing boundary is deliberate.

<CatTip>
Хороший Android target делает Android-слой тонким, но не искусственно пустым: lifecycle, resources, permissions and UI остаются там, где platform действительно важна.
</CatTip>

